<%- include('partials/header') %>
  <div class="container mt-4">
    <h2 style="font-size: 2.3em;">ALL</h2>
    <br>

    <h4>BLACK FRIDAY SALE: <b>30% to 70% OFF + Extra 15% off</b> </h4>
    <br>
    <br>
    <br>

    <div class="row">
      <% if (products.length===0) { %>
        <div class="col-12">
          <p>Məhsul tapılmadı.</p>
        </div>
        <% } %>
          <% products.forEach(function(item){ %>
            <div class="col-md-3 col-sm-6 mb-4">
              <div class="card h-100">
                <img src="/image/<%= item.image %>" class="card-img-top" alt="<%= item.productName %>">
                <div class="card-body d-flex flex-column">
                  <h5 class="card-title">
                    <%= item.productName %>
                  </h5>
                  <p class="card-text small">
                    <%= item.description %>
                  </p>
                  <div class="mt-auto">
                    <% if (item.apply==1) { %>
                      <p class="mb-1"><span class="text-muted text-decoration-line-through">
                          <%= item.price %>
                        </span></p>
                      <p class="mb-0"><strong class="text-danger">Endirim: <%= item.discount %></strong></p>
                      <% } else { %>
                        <p class="mb-0"><strong>Qiymət: <%= item.price %></strong></p>
                        <% } %>
                        <!--ADD TO CART-->

                          <a href="/add-to-cart/<%= item.productid %>" class="btn btn-dark w-100 mt-2">
                            Add to Cart 🛒
                          </a>
                  </div>
                </div>
              </div>
            </div>
            <% }) %>
    </div>
  </div>
  <%- include('partials/footer') %>